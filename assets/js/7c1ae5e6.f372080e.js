"use strict";(self.webpackChunkpioreactor=self.webpackChunkpioreactor||[]).push([[5856],{65165:(e,n,t)=>{t.r(n),t.d(n,{assets:()=>a,contentTitle:()=>r,default:()=>h,frontMatter:()=>s,metadata:()=>l,toc:()=>c});var i=t(85893),o=t(11151);const s={title:"Writing a custom background job",slug:"/writing-background-jobs"},r=void 0,l={id:"Background jobs/writing-background-jobs",title:"Writing a custom background job",description:"Introductory example",source:"@site/developer-guide/03-Background jobs/02-writing-background-jobs.md",sourceDirName:"03-Background jobs",slug:"/writing-background-jobs",permalink:"/developer-guide/writing-background-jobs",draft:!1,unlisted:!1,tags:[],version:"current",sidebarPosition:2,frontMatter:{title:"Writing a custom background job",slug:"/writing-background-jobs"},sidebar:"tutorialSidebar",previous:{title:"Introduction to background jobs",permalink:"/developer-guide/intro-background-jobs"},next:{title:"Avoiding when optical density is run",permalink:"/developer-guide/avoiding-od"}},a={},c=[{value:"Introductory example",id:"introductory-example",level:2},{value:"More advanced example",id:"more-advanced-example",level:2},{value:"Adding <code>published_settings</code>",id:"adding-published_settings",level:4}];function d(e){const n={a:"a",code:"code",em:"em",h2:"h2",h4:"h4",p:"p",pre:"pre",...(0,o.a)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(n.h2,{id:"introductory-example",children:"Introductory example"}),"\n",(0,i.jsxs)(n.p,{children:["For this introductory example, we'll create a background job that controls an LED in channel ",(0,i.jsx)(n.code,{children:"A"})," on the Pioreactor HAT. When the job ends (either by us exiting, or via a MQTT signal), the LED will turn off."]}),"\n",(0,i.jsx)(n.p,{children:"We start with some imports, and a class definition:"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-python",children:'from pioreactor.background_jobs.base import BackgroundJob\nfrom pioreactor.actions.led_intensity import led_intensity\n\nclass IntroJob(BackgroundJob):\n\n    job_name="intro_job"\n\n'})}),"\n",(0,i.jsxs)(n.p,{children:["We've given the class the simple name of ",(0,i.jsx)(n.code,{children:"IntroJob"}),". Our background job must inherit from ",(0,i.jsx)(n.code,{children:"BackgroundJob"}),". All jobs require a ",(0,i.jsx)(n.code,{children:"job_name"}),": the ",(0,i.jsx)(n.code,{children:"job_name"})," is usually the snake-case of the class name."]}),"\n",(0,i.jsx)(n.p,{children:"Next we'll define some initial attributes:"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-python",metastring:"{7-11}",children:'from pioreactor.background_jobs.base import BackgroundJob\nfrom pioreactor.actions.led_intensity import led_intensity\n\nclass IntroJob(BackgroundJob):\n\n    job_name="intro_job"\n    published_settings = {\n        \'intensity\': {\'datatype\': "float", "unit": "%", "settable": True}\n    }\n    intensity = 0\n    LED_channel = "A"\n'})}),"\n",(0,i.jsxs)(n.p,{children:["We declare that the ",(0,i.jsx)(n.code,{children:"intensity"})," attribute is to be published and controllable over MQTT. We'll see this in action later. We also set ",(0,i.jsx)(n.code,{children:"intensity"})," to be 0 initially, and use channel ",(0,i.jsx)(n.code,{children:"A"})," to control."]}),"\n",(0,i.jsxs)(n.p,{children:["Next we add the ",(0,i.jsx)(n.code,{children:"__init__"}),", which should always accept at least ",(0,i.jsx)(n.code,{children:"unit"})," and ",(0,i.jsx)(n.code,{children:"experiment"})," (as strings). ",(0,i.jsx)(n.code,{children:"unit"})," refers to the name of the Pioreactor (the hostname), and ",(0,i.jsx)(n.code,{children:"experiment"})," is the experiment the job is associated to."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-python",metastring:"{13,14}",children:'from pioreactor.background_jobs.base import BackgroundJob\nfrom pioreactor.actions.led_intensity import led_intensity\n\nclass IntroJob(BackgroundJob):\n\n    job_name="intro_job"\n    published_settings = {\n        \'intensity\': {\'datatype\': "float", "unit": "%", "settable": True}\n    }\n    intensity = 0\n    LED_channel = "A"\n\n    def __init__(self, unit, experiment):\n        super().__init__(unit=unit, experiment=experiment)\n\n'})}),"\n",(0,i.jsxs)(n.p,{children:["We call ",(0,i.jsx)(n.code,{children:"super"})," to initialize the super class, ",(0,i.jsx)(n.code,{children:"BackgroundJob"}),", with the unit, and experiment."]}),"\n",(0,i.jsxs)(n.p,{children:["Next, we define a function, ",(0,i.jsx)(n.code,{children:"set_intensity"})," that will be called whenever ",(0,i.jsx)(n.code,{children:"intensity"})," is changed remotely (we'll do this later). The ",(0,i.jsx)(n.code,{children:"set_intensity"})," function updates ",(0,i.jsx)(n.code,{children:"intensity"})," ",(0,i.jsx)(n.em,{children:"and"})," will change the onboard LED power for channel ",(0,i.jsx)(n.code,{children:"A"}),". Remember, whenever the attribute ",(0,i.jsx)(n.code,{children:"intensity"})," changes, it's published to MQTT."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-python",metastring:"{16-18}",children:'from pioreactor.background_jobs.base import BackgroundJob\nfrom pioreactor.actions.led_intensity import led_intensity\n\nclass IntroJob(BackgroundJob):\n\n    job_name="intro_job"\n    published_settings = {\n        \'intensity\': {\'datatype\': "float", "unit": "%", "settable": True}\n    }\n    intensity = 0\n    LED_channel = "A"\n\n    def __init__(self, unit, experiment):\n        super().__init__(unit=unit, experiment=experiment)\n\n    def set_intensity(self, intensity):\n        self.intensity = intensity\n        led_intensity({self.LED_channel: self.intensity})\n'})}),"\n",(0,i.jsxs)(n.p,{children:['Next, we create the "on exit" behaviour (turn off LED) by overwriting the ',(0,i.jsx)(n.code,{children:"on_disconnected"})," function."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-python",metastring:"{20,21}",children:'from pioreactor.background_jobs.base import BackgroundJob\nfrom pioreactor.actions.led_intensity import led_intensity\n\nclass IntroJob(BackgroundJob):\n\n    job_name="intro_job"\n    published_settings = {\n        \'intensity\': {\'datatype\': "float", "unit": "%", "settable": True}\n    }\n    intensity = 0\n    LED_channel = "A"\n\n    def __init__(self, unit, experiment):\n        super().__init__(unit=unit, experiment=experiment)\n\n    def set_intensity(self, intensity):\n        self.intensity = intensity\n        led_intensity({self.LED_channel: self.intensity})\n\n    def on_disconnected(self):\n        self.set_intensity(0)\n'})}),"\n",(0,i.jsx)(n.p,{children:"Finally, we add a small script at the bottom to run our new job when the Python file is invoked:"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-python",metastring:"{23-30}",children:'from pioreactor.background_jobs.base import BackgroundJob\nfrom pioreactor.actions.led_intensity import led_intensity\n\nclass IntroJob(BackgroundJob):\n\n    job_name="intro_job"\n    published_settings = {\n        \'intensity\': {\'datatype\': "float", "unit": "%", "settable": True}\n    }\n    intensity = 0\n    LED_channel = "A"\n\n    def __init__(self, unit, experiment):\n        super().__init__(unit=unit, experiment=experiment)\n\n    def set_intensity(self, intensity):\n        self.intensity = intensity\n        led_intensity({self.LED_channel: self.intensity})\n\n    def on_disconnected(self):\n        self.set_intensity(0)\n\nif __name__ == "__main__":\n    from pioreactor.whoami import get_unit_name\n    from pioreactor.whoami import get_assigned_experiment_name\n\n    unit = get_unit_name()\n    experiment = get_assigned_experiment_name(unit)\n\n    job = IntroJob(unit=unit, experiment=experiment)\n\n\n    job.block_until_disconnected()\n'})}),"\n",(0,i.jsxs)(n.p,{children:["If you save this code in a file called ",(0,i.jsx)(n.code,{children:"intro_job.py"}),", we can run it from the command line: ",(0,i.jsx)(n.code,{children:"python3 intro_job.py"}),"."]}),"\n",(0,i.jsx)(n.p,{children:"In another terminal window, try the following:"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:'pio mqtt -t "pioreactor/+/+/intro_job/#"\n'})}),"\n",(0,i.jsxs)(n.p,{children:["You should see some metadata about this job (",(0,i.jsx)(n.code,{children:"$state"})," is ",(0,i.jsx)(n.code,{children:"ready"}),", some info about ",(0,i.jsx)(n.code,{children:"intensity"}),"), but also you should see the current value of ",(0,i.jsx)(n.code,{children:"intensity"}),", namely 0. You can cancel the job with ",(0,i.jsx)(n.code,{children:"ctrl-c"})," in the original terminal window."]}),"\n",(0,i.jsxs)(n.p,{children:["Adding an additional line in ",(0,i.jsx)(n.code,{children:"intro_job.py"}),":"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-python",metastring:"{6}",children:'if __name__ == "__main__":\n    from pioreactor.whoami import get_unit_name\n    from pioreactor.whoami import get_assigned_experiment_name\n\n    unit = get_unit_name()\n    experiment = get_assigned_experiment_name(unit)\n\n    job = IntroJob(unit=unit, experiment=experiment)\n    job.set_intensity(10)\n\n    job.block_until_disconnected()\n'})}),"\n",(0,i.jsxs)(n.p,{children:["And rerunning the job, you should see MQTT have updated ",(0,i.jsx)(n.code,{children:"intensity"})," data."]}),"\n",(0,i.jsx)(n.h2,{id:"more-advanced-example",children:"More advanced example"}),"\n",(0,i.jsx)(n.p,{children:"We'll explain how a more advanced background job is written using the example application of a job that controls an external motor (the load). This motor may control a larger stirrer, or shaker, or air-pump, but is regulated by the amount of voltage applied - that is, the more voltage applied, the more output from the motor."}),"\n",(0,i.jsxs)(n.p,{children:["The Pioreactor HAT has four available pulse-width modulation (PWM) outputs, with programmable frequency and duty cycle (DC). For this example, we'll set a frequency that works for the motor. We also wish to increase the DC in proportion to the normalized optical density (this may represent needing to add more air, or shaking, as the culture's oxygen requirements increase). Let's start with the some imports and the class' ",(0,i.jsx)(n.code,{children:"__init__"}),":"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-python",children:'\nfrom pioreactor.background_jobs.base import BackgroundJob\n\n\nclass MotorDriver(BackgroundJob):\n\n    job_name="motor_driver"\n\n    def __init__(self, hz, initial_duty_cycle, unit, experiment, **kwargs):\n        super().__init__(unit=unit, experiment=experiment)\n        self.hz = hz\n        self._initial_duty_cycle = initial_duty_cycle\n        self.duty_cycle = initial_duty_cycle\n        ...\n\n'})}),"\n",(0,i.jsxs)(n.p,{children:["We see that the ",(0,i.jsx)(n.code,{children:"__init__"})," requires the two parameters for PWM: ",(0,i.jsx)(n.code,{children:"hz"})," and an ",(0,i.jsx)(n.code,{children:"inital_duty_cycle"}),". One rarely changes the hertz of PWM, so its fixed - but we do often change the duty cycle, so we create a variable ",(0,i.jsx)(n.code,{children:"self.duty_cycle"})," and assign it the ",(0,i.jsx)(n.code,{children:"initial_duty_cycle"}),". We also need to supply the unit name (the hostname), and the experiment name. We'll populate these at run time later. Finally, we need a ",(0,i.jsx)(n.code,{children:"job_name"})," to pass to the super class, ",(0,i.jsx)(n.code,{children:"BackgroundJob"}),'. This should be unique from other jobs that may run. Often we use the "snake_case" of the class name as the job name. We give it the job name ',(0,i.jsx)(n.code,{children:"motor_driver"}),"."]}),"\n",(0,i.jsxs)(n.p,{children:["We next initialize the PWM code (this is still in the ",(0,i.jsx)(n.code,{children:"__init__"}),") that controls the PWM outputs on the HAT, and add more imports:"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-python",children:'from pioreactor.hardware import PWM_TO_PIN\nfrom pioreactor.utils.pwm import PWM\nfrom pioreactor.config import config\n\n...\n\n\n    def __init__(...)\n        ...\n\n        pwm_pin = PWM_TO_PIN[config.get("PWM_reverse", "motor_driver")]\n        self.pwm = PWM(pwm_pin, self.hz)\n        self.pwm.lock()\n\n        ...\n\n'})}),"\n",(0,i.jsxs)(n.p,{children:["The ",(0,i.jsx)(n.code,{children:"PWM_TO_PIN"})," is lookup that maps settings in your config.ini to the Raspberry Pi's GPIO pins (which we use for the output PWM). Which means we also need update the ",(0,i.jsx)(n.code,{children:"[PWM]"})," section in your config.ini:"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"[PWM]\n1=motor_driver\n2=media\n3=alt_media\n4=waste\n5=heater\n"})}),"\n",(0,i.jsxs)(n.p,{children:["In the above code, we next initialize the ",(0,i.jsx)(n.code,{children:"PWM"})," class. This is an abstraction to make working with the PWM hardware easier. For example, the next line, ",(0,i.jsx)(n.code,{children:"self.pwm.lock()"}),", will put a lock on that Raspberry Pi GPIO pin, making it difficult for other processes to use it by mistake. We haven't started the PWM yet, we'll do that later."]}),"\n",(0,i.jsx)(n.p,{children:"Next, we want to include the behavior to update the duty cycle when we get new normalized OD readings. This is typically done with a callback when a new MQTT message is received. See code below:"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-python",children:'    def __init__(...)\n        ...\n        ...\n\n        self.start_passive_listeners()\n\n    ...\n\n    def start_passive_listeners(self):\n        self.logger.debug("Listening for od_filtered topics")\n        self.subscribe_and_callback(\n            self.update_duty_cycle_by_normalized_od,\n            f"pioreactor/{self.unit}/{self.experiment}/growth_rate_calculating/od_filtered",\n        )\n'})}),"\n",(0,i.jsxs)(n.p,{children:["Note the ",(0,i.jsx)(n.code,{children:"self.logger.debug(...)"})," line. Each background job has a Python logger that will log to four places: MQTT, the SQLite3 database, the terminal, and the log file on disk. See Python's ",(0,i.jsx)(n.a,{href:"https://docs.python.org/3/library/logging.html",children:"logging module"})," for more."]}),"\n",(0,i.jsxs)(n.p,{children:["The callback, ",(0,i.jsx)(n.code,{children:"update_duty_cycle_by_normalized_od"}),", needs to be written:"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-python",children:'import json\n\n...\n\n    def update_duty_cycle_by_normalized_od(self, message):\n        payload = json.loads(message.payload)\n\n        if self.state == self.READY:\n            self.set_duty_cycle(payload["od_filtered"] * self._initial_duty_cycle)\n\n    def set_duty_cycle(self, new_duty_cycle):\n        self.duty_cycle = clamp(0, float(new_duty_cycle), 100)\n        self.pwm.change_duty_cycle(self.duty_cycle)\n        self.logger.debug(f"new dc: {self.duty_cycle}")\n\n'})}),"\n",(0,i.jsxs)(n.p,{children:["In the callback, ",(0,i.jsx)(n.code,{children:"update_duty_cycle_by_normalized_od"}),", we accept the message object. The message has two important properties: ",(0,i.jsx)(n.code,{children:"topic"})," and ",(0,i.jsx)(n.code,{children:"payload"}),". We only care about the payload, which is a string of json. We de-serialize the json to a dict."]}),"\n",(0,i.jsxs)(n.p,{children:["We only want to update the duty cycle if the job is in state ",(0,i.jsx)(n.code,{children:"READY"})," (read about states ",(0,i.jsx)(n.a,{href:"/developer-guide/intro-background-jobs#state-of-a-job",children:"here"}),"). If this is true, we call ",(0,i.jsx)(n.code,{children:"set_duty_cycle"})," to update our duty cycle. This method handles the logic of clamping the value to be between 0 and 100, and we've stuck a ",(0,i.jsx)(n.code,{children:"debug"})," in there so we can watch it change."]}),"\n",(0,i.jsxs)(n.p,{children:["What is the relationship between normalized OD and duty cycle. We do something naive, and just multiple the initial duty cycle by the normalized OD. So if the ",(0,i.jsx)(n.code,{children:"initial_duty_cycle"})," is 10%, and the culture has 3.5x, then our new duty cycle is 35%. It ",(0,i.jsx)(n.em,{children:"is"})," very naive."]}),"\n",(0,i.jsx)(n.p,{children:"Finally, we need to think about changing states. What should our job do when the user pauses the job? How do we safely disconnect from the PWM? We use the state callback methods to handle these changes:"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-python",children:"    def on_init_to_ready(self):\n        self.pwm.start(self.duty_cycle)\n\n    def on_ready_to_sleeping(self):\n        self._previous_duty_cycle = self.duty_cycle\n        self.set_duty_cycle(0)\n\n    def on_sleeping_to_ready(self):\n        self.set_duty_cycle(self._previous_duty_cycle)\n\n    def on_disconnected(self):\n        self.pwm.clean_up()\n"})}),"\n",(0,i.jsxs)(n.p,{children:["After the job moves from ",(0,i.jsx)(n.code,{children:"init"})," to ",(0,i.jsx)(n.code,{children:"ready"})," (implicitly done after the ",(0,i.jsx)(n.code,{children:"__init__"})," finishes), the function ",(0,i.jsx)(n.code,{children:"on_init_to_ready"})," is called. This starts the PWM, which starts the motor."]}),"\n",(0,i.jsxs)(n.p,{children:["When we sleep (pause), we record the last ",(0,i.jsx)(n.code,{children:"duty_cycle"})," value, and use that to populate the ",(0,i.jsx)(n.code,{children:"duty_cycle"})," when we re-start the job."]}),"\n",(0,i.jsxs)(n.p,{children:["Let's recap what we have so far, and save this to a file called ",(0,i.jsx)(n.code,{children:"motor_driver.py"})," (ideally we save it in ",(0,i.jsx)(n.code,{children:"/home/pioreactor/.pioreactor/plugins/"}),":"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-python",children:'# -*- coding: utf-8 -*-\nimport json\n\nfrom pioreactor.config import config\nfrom pioreactor.background_jobs.base import BackgroundJob\nfrom pioreactor.hardware import PWM_TO_PIN\nfrom pioreactor.utils.pwm import PWM\nfrom pioreactor.utils import clamp\n\n\nclass MotorDriver(BackgroundJob):\n\n    job_name="motor_driver"\n    published_settings = {\n        "duty_cycle": {"datatype": "float", "settable": False, "unit": "%"},\n    }\n    _previous_duty_cycle: None\n\n    def __init__(self, hz, initial_duty_cycle, unit, experiment, **kwargs):\n        super().__init__(unit=unit, experiment=experiment)\n        self.hz = hz\n        self._initial_duty_cycle = initial_duty_cycle\n        self.duty_cycle = initial_duty_cycle\n\n        self.pwm_pin = PWM_TO_PIN[config.get("PWM_reverse", "motor_driver")]\n\n        self.pwm = PWM(self.pwm_pin, self.hz)\n        self.pwm.lock()\n\n        self.start_passive_listeners()\n\n    def update_duty_cycle_by_normalized_od(self, message):\n        payload = json.loads(message.payload)\n\n        if self.state == self.READY:\n            self.set_duty_cycle(payload["od_filtered"] * self._initial_duty_cycle)\n\n    def set_duty_cycle(self, new_duty_cycle):\n        self.duty_cycle = clamp(0, float(new_duty_cycle), 100)\n        self.pwm.change_duty_cycle(self.duty_cycle)\n        self.logger.debug(f"new dc: {self.duty_cycle}")\n\n    def start_passive_listeners(self):\n        self.logger.debug("Listening for od_filtered topics")\n        self.subscribe_and_callback(\n            self.update_duty_cycle_by_normalized_od,\n            f"pioreactor/{self.unit}/{self.experiment}/growth_rate_calculating/od_filtered",\n        )\n\n    def on_init_to_ready(self):\n        self.pwm.start(self.duty_cycle)\n\n    def on_ready_to_sleeping(self):\n        self._previous_duty_cycle = self.duty_cycle\n        self.set_duty_cycle(0)\n\n    def on_sleeping_to_ready(self):\n        self.set_duty_cycle(self._previous_duty_cycle)\n\n    def on_disconnected(self):\n        self.logger.debug("disconnecting... will clean up PWM")\n        self.pwm.clean_up()\n'})}),"\n",(0,i.jsxs)(n.p,{children:["This class works as is, but we also want to develop a command line interface for it so we can run it like ",(0,i.jsx)(n.code,{children:"pio run motor_driver"}),"."]}),"\n",(0,i.jsx)(n.p,{children:"At the bottom of the file, we add:"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-python",children:'import click\n\n@click.command(name="motor_driver")\n@click.option(\n    "--initial-dc",\n    default=config.getfloat("motor_driver", "initial_duty_cycle"),\n    show_default=True,\n    type=click.FloatRange(0, 100, clamp=True),\n)\n@click.option(\n    "--hz",\n    default=config.getfloat("motor_driver", "hz"),\n    show_default=True,\n    type=click.FloatRange(1, 10_000, clamp=True),\n)\ndef click_motor_driver(initial_dc, hz):\n    """\n    Start the external motor\n    """\n    from pioreactor.whoami import get_unit_name, get_assigned_experiment_name\n\n    unit = get_unit_name()\n    experiment = get_assigned_experiment_name(unit)\n\n    job = MotorDriver(\n        hz=hz,\n        initial_duty_cycle=initial_dc,\n        unit=unit,\n        experiment=experiment,\n    )\n    job.block_until_disconnected()\n'})}),"\n",(0,i.jsxs)(n.p,{children:["Note the helper functions ",(0,i.jsx)(n.code,{children:"get_unit_name"})," and ",(0,i.jsx)(n.code,{children:"get_assigned_experiment_name"})," to get the metadata for the class. The method ",(0,i.jsx)(n.code,{children:"block_until_disconnected"})," will halt the program at that line (and only continue when a keyboard interrupt is detected)."]}),"\n",(0,i.jsxs)(n.p,{children:["If you save it in the ",(0,i.jsx)(n.code,{children:"plugins"})," folder, you can now execute: ",(0,i.jsx)(n.code,{children:"pio run motor_driver --initial-dc 10"})," and it should just work! You can exit with ctrl-c, and note that the ",(0,i.jsx)(n.code,{children:"on_disconnect"})," is called when you do this."]}),"\n",(0,i.jsxs)(n.h4,{id:"adding-published_settings",children:["Adding ",(0,i.jsx)(n.code,{children:"published_settings"})]}),"\n",(0,i.jsxs)(n.p,{children:["Suppose we wish to monitor, or log, or just otherwise publish the ",(0,i.jsx)(n.code,{children:"duty_cycle"})," from the ",(0,i.jsx)(n.code,{children:"MotorDriver"}),' class. We can "register" the attribute ',(0,i.jsx)(n.code,{children:"duty_cycle"})," in the ",(0,i.jsx)(n.code,{children:"published_settings"}),":"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-python",metastring:"{4,5,6}",children:'class MotorDriver(BackgroundJob):\n    ...\n\n    published_settings = {\n        "duty_cycle": {"datatype": "float", "settable": False, "unit": "%"},\n    }\n\n    def __init__(...)\n\n'})}),"\n",(0,i.jsxs)(n.p,{children:["If you open up the MQTT stream using ",(0,i.jsx)(n.code,{children:"pio mqtt"}),", you should see ",(0,i.jsx)(n.code,{children:"duty_cycle"})," appear under the topic ",(0,i.jsx)(n.code,{children:"pioreactor/<unit>/<experiment>/motor_driver/duty_cycle"}),"."]})]})}function h(e={}){const{wrapper:n}={...(0,o.a)(),...e.components};return n?(0,i.jsx)(n,{...e,children:(0,i.jsx)(d,{...e})}):d(e)}},11151:(e,n,t)=>{t.d(n,{Z:()=>l,a:()=>r});var i=t(67294);const o={},s=i.createContext(o);function r(e){const n=i.useContext(s);return i.useMemo((function(){return"function"==typeof e?e(n):{...n,...e}}),[n,e])}function l(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(o):e.components||o:r(e.components),i.createElement(s.Provider,{value:n},e.children)}}}]);